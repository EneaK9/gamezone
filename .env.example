# TypeSafe API key for NPC conversations (Jev). Get one at https://console.typesafe.ai/keys
# Only the dev server / serverless function reads it; it is never sent to the browser.
# Without a key the game still works: NPCs fall back to a simple keyword interpreter.
TYPESAFE_API_KEY=
